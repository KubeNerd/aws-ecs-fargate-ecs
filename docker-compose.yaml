version: '3'
networks:
  ecs-network:
    driver: bridge
    
services:
  api:
    container_name: backend_api_dogs
    build: .
    ports:
      - 3333:${APP_PORT}
    networks:
      - ecs-network
    